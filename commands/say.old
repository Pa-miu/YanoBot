const Discord = require('discord.js');

module.exports.run = async (bot, message, args) => {
	return message.channel.send(args.join(' '));
};

module.exports.help = {
	name: 'say',
	usage: '!say <write stuff here>',
	description: 'Parrots whatever you tell it to'
};